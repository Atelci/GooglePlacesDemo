{
  "name": "googlePlacesDemo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "mongoose": "^5.4.10",
    "react": "^16.8.1",
    "react-dom": "^16.8.1",
    "react-scripts": "2.1.3"
  },
  "scripts": {
    "start": "set PORT=8070 && react-scripts start && node server.js",
    "dev": "set PORT=8070 && react-scripts start && nodemon server.js",
    "build": "react-scripts build",
    "server": "node-env-run server --exec nodemon"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "node-env-run": "^3.0.2"
  }
}
